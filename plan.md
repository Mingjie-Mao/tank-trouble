# 项目规划：类 Tank Trouble 深度学习坦克 AI

## 0. 项目目标

我要构建一个 **类似 Tank Trouble 的 2D 坦克对战环境**，并训练一个深度学习智能体，使它最终能够与原版 Tank Trouble 的 AI **Laika** 对抗，至少达到可竞争水平。

重要约束：

- 我没有原版 Tank Trouble 源代码。
- 我不知道 Laika 的内部实现。
- 我拿不到 Laika 的动作标签。
- 因此，Laika 只能被视作 **黑盒基准**，不能被当作可直接克隆的老师。

最终目标不是精确复刻原版游戏，而是构建一个“足够相似”的环境，让神经网络学会可迁移的坦克战斗能力：

- 迷宫导航
- 搜敌
- 瞄准
- 子弹反弹利用
- 躲避子弹
- 墙体碰撞处理
- 战术站位
- 生存与击杀优化

最终训练流程应当是：

```text
规则老师 TeacherBot
→ 行为克隆 Behavior Cloning
→ DAgger 式纠正
→ 强化学习微调
→ 自博弈
→ 域随机化
→ 与原版 Laika 黑盒对测
```

---

## 1. 高层设计哲学

不要从“纯随机策略 + 纯强化学习”开始。

纯 RL 会把大量时间浪费在低级行为上，例如：

- 不要原地乱转
- 不要一直撞墙
- 不要无意义开火
- 不要卡住
- 要靠近敌人
- 要躲明显会打中的子弹

更合理、也更有研究价值的路线是：

1. 先搭一个类 Tank Trouble 模拟器。
2. 写一个强规则老师 **TeacherBot**，模拟 Laika 可能具备的能力。
3. 用 TeacherBot 做行为克隆。
4. 用 DAgger 修正模型在分布外状态下的错误。
5. 再用强化学习和自博弈超过 TeacherBot。
6. 用域随机化提升泛化能力。
7. 最后把 Laika 当成外部黑盒考试对象。

---

## 2. 非目标

不要做下面这些事：

- 复制原版 Tank Trouble 源码
- 使用原版受版权保护的素材
- 激进逆向原版专有实现
- 强行复刻 Laika 的内部逻辑
- 以录像监督作为主要训练路线
- 做一个视觉上一模一样的克隆

更准确的项目定位应当是：

> 一个研究导向的、受 Tank Trouble 启发的强化学习坦克对战环境，用于训练和评估神经网络坦克战斗智能体。

---

## 3. 环境需求

需要构建一个俯视角 2D 坦克战斗环境，至少支持：

### 3.1 核心机制

- 矩形 2D 战场
- 迷宫式墙体布局
- 两辆坦克
- 坦克旋转
- 前进 / 后退
- 墙体碰撞
- 子弹发射
- 子弹墙体反弹
- 子弹寿命或最大反弹次数
- 被子弹击中即死亡
- 死亡后回合重置
- 随机出生点
- 多张随机地图

### 3.2 建议环境接口

环境接口尽量做成 Gymnasium 风格：

```python
obs = env.reset()
obs, reward, terminated, truncated, info = env.step(action)
env.render()
```

这样后续更容易对接 Stable-Baselines3 或自定义 PPO / DQN。

---

## 4. 观测空间

优先用结构化状态观测，不要一开始就用纯像素。

观测建议包含：

### 自己的坦克

- x 位置
- y 位置
- 朝向角
- 线速度
- 角速度
- 射击冷却

### 敌方坦克

- 相对 x
- 相对 y
- 相对朝向
- 与敌人的距离
- 指向敌人的方位角

### 子弹

对每颗可见子弹，或最近的前 K 颗子弹记录：

- 相对 x
- 相对 y
- 速度 x
- 速度 y
- 距离
- 可能的话估计碰撞时间

### 墙体 / 地图几何

- 多方向射线探测距离
- 前方墙体距离
- 左侧墙体距离
- 右侧墙体距离
- 后方墙体距离
- 可选的局部占据网格

### 战斗几何特征

- 是否存在直射线
- 是否存在一次反弹射击机会
- 是否存在两次反弹射击机会
- 可选的近似最佳瞄准角

推荐初始方案：

```text
向量观测 + 射线特征
```

后续可升级为：

```text
局部占据网格 + 向量特征
```

---

## 5. 动作空间

优先使用离散或 MultiDiscrete 动作，不要一开始就上连续控制。

推荐三头动作结构：

```text
移动：
- 前进
- 后退
- 不动

转向：
- 左转
- 右转
- 不转

开火：
- 开火
- 不开火
```

可表示为：

```python
MultiDiscrete([3, 3, 2])
```

也可以展开成一个更大的离散动作集合。

---

## 6. TeacherBot 设计

因为原版 Laika 不可得，所以必须自己实现一个规则老师 **TeacherBot**。

TeacherBot 是第一阶段模仿学习的专家策略。

它应采用优先级式行为系统：

```text
优先级 1：如果存在高命中率射击机会，则优先射击
优先级 2：如果有危险来弹，则优先躲弹
优先级 3：如果卡住或即将被困，则优先脱困
优先级 4：否则使用路径规划追击敌人
优先级 5：如果没有更明确行为，则进行战术重定位
```

### 6.1 射击模块

基于射线检测进行开火决策。

TeacherBot 应检查：

- 直射机会
- 一次反弹机会
- 两次反弹机会（如果可行）
- 当前炮口角度是否接近理想开火角
- 冷却是否结束

输出：

```text
左转 / 右转 / 开火 / 按兵不动
```

### 6.2 躲弹模块

TeacherBot 应预测子弹轨迹：

- 短时间模拟子弹未来路径
- 考虑墙体反弹
- 判断是否会击中坦克
- 若危险，则选择逃逸方向

优先策略：

- 沿来弹方向的垂直方向躲避
- 借助墙体做掩体
- 避开角落
- 避免把自己困死

### 6.3 路径规划模块

在离散化地图上使用 A* 或 BFS。

目的：

- 接近敌人
- 避开墙体
- 避免卡住
- 重定位到更有利的射击位置

不要求绝对最优，但必须足够好，能产出有用的模仿学习数据。

### 6.4 卡住恢复模块

通过检测最近若干帧位移是否过小判断卡住：

```text
最近若干帧位置变化 < 阈值
```

一旦卡住：

- 倒车
- 转向
- 改换方向
- 暂时忽略追击逻辑

---

## 7. 训练阶段 1：行为克隆

先训练一个神经策略去模仿 TeacherBot。

### 7.1 数据采集

让 TeacherBot 对战以下对手并大量采样：

```text
TeacherBot vs TeacherBot
TeacherBot vs RandomBot
TeacherBot vs WeakBot
TeacherBot vs 简单追击 bot
```

对每个决策时刻记录：

```text
观测 s_t
TeacherBot 动作 a_t
游戏元数据
地图 id
出生点
回合结果
```

建议保存到：

```text
datasets/teacher_bc/
```

推荐格式：

```text
.npz / .parquet / .pt
```

### 7.2 监督学习目标

训练策略网络：

```text
pi_theta(a | s)
```

去预测 TeacherBot 的动作。

损失函数：

```text
离散动作交叉熵
```

如果采用多头动作：

```text
loss = movement_loss + rotation_loss + fire_loss
```

期望结果：

- 模型不会老是撞墙
- 会朝敌人移动
- 会进行粗略瞄准
- 会在合理时机开火
- 会躲避简单威胁

这一阶段的目标不是超过 TeacherBot，而是避免 RL 从纯随机策略开始。

---

## 8. 训练阶段 2：DAgger 式数据聚合

行为克隆会有分布偏移问题：

- TeacherBot 只会产生它自己访问过的状态
- 神经模型会犯小错
- 小错会把它带入老师没见过的状态
- 在这些状态下，模型可能崩得很厉害

所以需要 DAgger。

### 8.1 DAgger 循环

反复执行：

```text
1. 让神经策略自己玩
2. 记录神经策略访问到的状态
3. 让 TeacherBot 给这些状态打标签
4. 把新状态-动作对加入数据集
5. 重新训练或微调策略
```

特别重要的状态包括：

- 靠墙卡住
- 与敌人角度很差
- 子弹从奇怪方向飞来
- 角落陷阱
- 瞄准失败
- 过度旋转
- 错误开火时机

期望结果：

模型能更稳地处理“由自己犯错导致”的坏状态。

---

## 9. 训练阶段 3：强化学习微调

当模仿策略已经具备基础能力后，再接强化学习。

不要从头开始 RL，要从行为克隆模型初始化。

推荐算法：

- PPO：最稳定，最容易起步
- DQN / Rainbow：适合离散大动作空间
- DQfD：适合演示数据 + RL 混合

首选 PPO。

### 9.1 奖励函数

主奖励：

```text
赢一局：+1.0
输一局：-1.0
击中敌人：+0.5
自己死亡：-0.5
平局：0.0
```

辅助奖励要小：

```text
成功躲避：+0.02
向有效位置移动：+0.005
制造出有效射击机会：+0.01
卡住：-0.01
朝明显危险方向开火：-0.01
```

核心原则：

辅助奖励必须始终小于“胜负奖励”，避免模型只会刷 shaping reward，不关心胜率。

最终目标仍然是：

```text
最大化胜率
```

---

## 10. 训练阶段 4：自博弈与对手池

如果要超过 TeacherBot，必须用多样化对手池训练。

对手池建议包括：

```text
RandomBot
WeakBot
TeacherBot
旧版本神经策略
当前神经策略
偏防守 bot
偏进攻 bot
```

不要只对战 TeacherBot，否则模型会只学 TeacherBot 的弱点。

训练中维护历史 checkpoint：

```text
checkpoints/policy_v001.pt
checkpoints/policy_v002.pt
...
```

并定期评估：

- 对 TeacherBot 的胜率
- 对旧 checkpoint 的胜率
- 对弱 / 随机 bot 的胜率
- 类 Elo 指标

---

## 11. 域随机化

因为我们做的不是原版 Tank Trouble，因此必须用域随机化增强迁移能力。

训练中可以随机化：

- 坦克速度 ±10%
- 转向速度 ±10%
- 子弹速度 ±10%
- 子弹半径 ±5%
- 墙体厚度 ±5%
- 碰撞摩擦 ±10%
- 开火冷却 ±10%
- 子弹反弹次数
- 地图大小
- 墙体布局
- 出生点
- 输入延迟 0 到 3 帧
- 观测噪声

目标是让模型学到的是“战斗原则”，而不是死记某一个固定模拟器。

---

## 12. 评估指标

不要靠一两局判断模型优劣。

### 12.1 内部评估

对 TeacherBot：

- 1000 局胜率
- 平均回合时长
- 击杀 / 死亡比
- 自杀率
- 卡住频率
- 平均击杀时间
- 平均存活时间

对自博弈池：

- Elo 风格评分
- 对旧版本胜率
- 对随机 / 弱 bot 胜率
- 对防守 / 进攻专用 bot 胜率

### 12.2 对原版 Laika 的外部评估

Laika 只能被当成黑盒考试对象。

如果未来能接入自动化，对测至少要有：

- 最少 100 局
- 最好 500 局以上

记录：

- 对 Laika 胜率
- 平均存活时间
- 击杀数
- 死亡数
- 自杀次数
- 躲弹成功情况
- 失败模式

如果打不过，就把失败局面抽象成训练场景，再回灌到模拟器里继续训练。

---

## 13. 失败驱动训练

把失败分成若干类：

```text
A. 导航失败
B. 卡住
C. 找不到敌人
D. 瞄准太慢
E. 开火太早
F. 不会利用反弹
G. 死于直射
H. 死于反弹弹道
I. 自杀
J. 对单一地图过拟合
```

对每一类失败：

1. 设计一个能复现该问题的场景生成器
2. 把这类场景加入训练
3. 若是模仿学习阶段，则让 TeacherBot 给标签
4. 若需要超过老师，则用 RL 微调

---

## 14. 模型结构建议

先从简单模型开始。

### 14.1 基础 MLP 策略

用于向量观测时：

```text
输入：观测向量
隐藏层：2 到 4 层
宽度：128 到 512
激活：ReLU 或 GELU
输出：动作 logits
```

多头动作可做成：

```text
共享主干
→ 移动头
→ 转向头
→ 开火头
```

如果用 PPO，则使用 Actor-Critic：

```text
策略 logits
状态价值 V(s)
```

### 14.2 后续升级

如果后面加入局部网格输入：

```text
局部占据网格用 CNN 编码
向量特征用 MLP 编码
两者拼接
输出策略头和值函数头
```

---

## 15. 推荐目录结构

```text
tank_ai_project/
├── env/
│   ├── tank_env.py
│   ├── physics.py
│   ├── maps.py
│   ├── collision.py
│   ├── bullets.py
│   └── rendering.py
│
├── bots/
│   ├── random_bot.py
│   ├── weak_bot.py
│   ├── teacher_bot.py
│   ├── pathfinding.py
│   ├── shooting.py
│   └── dodging.py
│
├── data/
│   ├── collect_teacher_data.py
│   ├── collect_dagger_data.py
│   └── datasets/
│
├── models/
│   ├── policy.py
│   ├── actor_critic.py
│   └── checkpoints/
│
├── training/
│   ├── train_bc.py
│   ├── train_dagger.py
│   ├── train_ppo.py
│   └── self_play.py
│
├── evaluation/
│   ├── evaluate_vs_teacher.py
│   ├── evaluate_vs_pool.py
│   ├── analyze_failures.py
│   └── replay_viewer.py
│
├── configs/
│   ├── env_default.yaml
│   ├── bc.yaml
│   ├── dagger.yaml
│   ├── ppo.yaml
│   └── domain_randomization.yaml
│
├── notebooks/
│   └── analysis.ipynb
│
├── README.md
└── requirements.txt
```

---

## 16. 里程碑

### 里程碑 1：可玩的模拟器

交付内容：

- 坦克移动
- 墙体碰撞
- 子弹发射
- 子弹反弹
- 回合结束
- 可视化渲染

成功标准：

```text
人类或脚本 bot 能完整打完回合
```

### 里程碑 2：TeacherBot

交付内容：

- 路径规划
- 射击射线检测
- 躲弹
- 卡住恢复
- 优先级行为系统

成功标准：

```text
TeacherBot 能稳定打赢 RandomBot 和 WeakBot
```

### 里程碑 3：行为克隆

交付内容：

- TeacherBot 数据采集
- 神经策略模型
- 监督训练脚本
- 对 RandomBot / WeakBot / TeacherBot 的评估

成功标准：

```text
行为克隆策略明显优于随机策略，并具备基础战斗行为
```

### 里程碑 4：DAgger

交付内容：

- 神经策略 rollout 采集
- TeacherBot 重新标注
- 聚合数据集训练

成功标准：

```text
DAgger 策略比纯 BC 更少卡住、更少明显送死、胜率更高
```

### 里程碑 5：强化学习微调

交付内容：

- PPO 或 DQN 训练
- 奖励日志
- checkpoint 保存
- 评估脚本

成功标准：

```text
RL 微调后的策略超过纯 TeacherBot 模仿
```

### 里程碑 6：自博弈与域随机化

交付内容：

- 对手池
- 历史 checkpoint
- 随机物理 / 地图
- 稳健性评估

成功标准：

```text
模型在多种地图和物理条件下都能保持较强表现
```

### 里程碑 7：对 Laika 黑盒评估

交付内容：

- 对原版 Laika 的手动或自动评估
- 结果表
- 失败分析

成功标准：

```text
模型能和 Laika 有竞争力，或至少给出明确失败画像与下一步训练方向
```

---

## 17. 学术表述

这个项目可以被描述为：

> 一个结合模仿学习与强化学习的类 Tank Trouble 多智能体战斗训练方案。由于原版 Laika AI 不可获得，且只能被当作黑盒对手，项目先构造一个受 Laika 行为启发的规则专家，再通过行为克隆蒸馏成神经策略，并借助 DAgger 提高鲁棒性，随后用强化学习、自博弈和域随机化进一步提升，最终以 Laika 作为外部黑盒基准进行评估。

相关概念包括：

- 模仿学习
- 行为克隆
- 策略蒸馏
- DAgger
- 演示辅助强化学习
- 自博弈
- 课程学习
- 域随机化
- 奖励塑形
- 黑盒基准评估

---

## 18. 核心实现原则

最重要的一条原则是：

```text
不要试图完美复制 Laika。
要训练一个足够泛化、足够强的坦克战斗策略，去打赢 Laika。
```

Laika 不是训练来源。

Laika 是最终考试。

训练来源应当是：

```text
我们的模拟器
+ 我们自己的 TeacherBot
+ 模仿学习
+ DAgger
+ 强化学习
+ 自博弈
+ 域随机化
```

---

## 19. 当前应优先开始的任务

应该先做：

```text
1. 构建一个最小可用的 Gymnasium 风格 2D 坦克环境
2. 实现坦克移动、旋转、墙体碰撞、子弹反弹
3. 实现 RandomBot 和 WeakBot
4. 实现 TeacherBot v1：
   - 有直射机会就瞄准并开火
   - 否则若有危险子弹就躲
   - 否则路径规划追击敌人
   - 否则重定位
5. 加入 TeacherBot 数据采集
6. 训练第一版行为克隆模型
7. 在 RandomBot / WeakBot / TeacherBot 上评估 BC 模型
```

不要一开始就把主要精力放在 PPO 或复杂 RL 上。

第一个成功原型应证明：

```text
TeacherBot 能正常对战
TeacherBot 数据可采集
神经策略能模仿 TeacherBot
神经策略能打赢 RandomBot
```
